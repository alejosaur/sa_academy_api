"main";let ApolloServer,gql;_1c0‍.w("apollo-server",[["ApolloServer",["ApolloServer"],function(v){ApolloServer=v}],["gql",["gql"],function(v){gql=v}]]);let ResidentAPI,ReportsAPI;_1c0‍.w("./datasource",[["ResidentAPI",["ResidentAPI"],function(v){ResidentAPI=v}],["ReportsAPI",["ReportsAPI"],function(v){ReportsAPI=v}]]);yield;


const { find, filter } = require('lodash');

const typeDefs = gql`
    type Resident {
        _id: ID!
        identity_document: Int
        name: String!
        birth_date: String
        admission_date: String!
        gender: String!
        state: String
        contact_name: String
        contact_phone: Int
        diseases: String
    }

    type Report_type {
        id: ID!
        Type: String!
        reports: [Report]
    }

    type Report {
        id: ID!
        report_type_id: Report_type
        created_at: String
        IP: String
    }

    type Query {
        resident(idNumber: String!): Resident
        residents: [Resident]
        report(idNumber: Int!): Report
        reports: [Report]
        report_types: [Report_type]
    }
`;

const resolvers = {
    Query: {
        resident: (root, { idNumber }, { dataSources }) => dataSources.ResidentAPI.getAResident(idNumber),
        residents: (root, args, { dataSources }) => dataSources.ResidentAPI.getAllResidents(),
        report: (root, { idNumber }, { dataSources }) => dataSources.ReportsAPI.getAReport(idNumber),
        reports: (root, args, { dataSources }) => dataSources.ReportsAPI.getAllReports(),
        report_types: (root, args, { dataSources }) => dataSources.ReportsAPI.getAllTypes(),
    },
    Report_type: {
        report(report_type){
            return find(reports, { id: args.id });
        }
    },
    Resident: {
        identity_document: ({ idDoc }) => idDoc,
        diseases: ({ reportedDiseases }) => reportedDiseases,
    },
};

const server = new ApolloServer({
    typeDefs,
    resolvers,
    dataSources: () => ({
        ResidentAPI: new ResidentAPI(),
        ReportsAPI: new ReportsAPI(),
    }),
});

server.listen().then(({ url }) => {
    _1c0‍.g.console.log(`🚀 Server ready at ${url}`);
});

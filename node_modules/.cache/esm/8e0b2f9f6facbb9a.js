"main";_6fa‍.x([["MvrpAPI",()=>MvrpAPI]]);let RESTDataSource;_6fa‍.w("apollo-datasource-rest",[["RESTDataSource",["RESTDataSource"],function(v){RESTDataSource=v}]]);yield;

       class MvrpAPI extends RESTDataSource {
  constructor() {
    super();
    this.baseURL = 'localhost:3000/';
  }

  async getAllCars() {
    return this.get('residents');
  }

  async getACar(plateNumber) {
    const result = await this.get('resident', {
      idNumber
    });

    return result;
  }
};
